@{
    Layout = null;
}
@model MerchantTribe.Commerce.Content.ContentColumn
@using MerchantTribe.Commerce.Contacts;

@foreach (var b in Model.Blocks)
{
      @Html.Action("Index", b.ControlName.Replace(" ", ""), 
                new { Area = "ContentBlocks", block = b })     
}
